# This is a fully STATIC Astro site — no server secrets are required to build
# or run it. Newsletter and contact submissions go to Formspree, configured in
# src/consts.js (FORMS.newsletterEndpoint / FORMS.contactEndpoint).
#
# You normally do NOT need a .env file. Keep this only if you later add a
# build-time integration that needs a key.
